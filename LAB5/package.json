{
  "name": "tsp-genetic-algorithm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "install:all": "npm install && cd frontend && npm install",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && uvicorn app.main:app --reload\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.9.0"
  }
}
